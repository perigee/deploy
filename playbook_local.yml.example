---

- hosts: localhost
  sudo: yes
  vars:
    proxy_env:
      http_proxy: http://proxy-mtl.ubisoft.org:3128
      https_proxy: http://proxy-mtl.ubisoft.org:3128
  vars_files:
    - vars/basic.yml
  roles:
    - common

  environment: "{{ proxy_env }}"
  